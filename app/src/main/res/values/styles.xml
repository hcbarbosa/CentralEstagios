<resources>

    <!-- Base application theme. -->
    <style name="Theme.AppCompat.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/colorGray</item>
        <item name="android:colorAccent">@color/colorLogo</item>
    </style>

    <style name="MyToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/colorBlack</item>
    </style>
</resources>